# Normalize line endings: always check in LF so Windows git clones
# (core.autocrlf) cannot corrupt the sources.
* text=auto eol=lf

# Scripts distributed for Windows must stay LF on disk too.
*.ps1 text eol=lf
*.cmd text eol=lf
*.bat text eol=lf